@model Models.Entities.Item

@{
    ViewBag.Title = "Login user";
    Layout = "_Layout";
}

@using (Html.BeginForm("EditHandler", "Item", FormMethod.Post, new { }))
{
    @Html.Partial("Contents/ItemContent", Model, new ViewDataDictionary(ViewData) {{ "Type", "Edit"}})

    <input class="btn btn-primary" type="submit" value="Update"/>
}